# Amalgam Audit Findings — 2026-08-31

This file records the evidence from the latest build, automated test run, live desktop pass, and package checks. It is audit data, not implementation instructions.

## 2026-09-22 expanded V2 completion pass — new P0/P1 evidence

- **Fixture isolation P1 found and contained:** the `cloud` visual-fixture route reached `draw_cloud_page`, which synchronously requested live plans and server lists. The incomplete full matrix was stopped before Cloud; `cloud_ui.cpp` now has a fixture-first local facade and its exact snapshot smoke produced a valid `1242x610` PNG without changing `launcher.json`. A new full run is required after all remaining safety changes.
- **High-impact Admin action P1s:** Stop/Restart/Delete server, Deregister node, and project Approve/Reject/Take Down were one-click operations. Stop/restart can interrupt players and the underlying local stop path has a force-termination fallback. Storage bucket Delete is a currently nonfunctional no-op and must be visibly unavailable rather than enabled. A typed copied-data `AdminPendingAction` confirmation boundary is being wired, including target validation, typed-name/reason/acknowledgement requirements, expiry revalidation, and precise notices.
- **Admin scope/trust P1:** the Admin UI claims fleet-level management while server/node mutations currently filter by the current user identifier. Client-side unlock is not an authorization boundary. Management copy and backend authorization must be reconciled before presenting this as staff fleet control.
- **Destructive action sweep continues:** source evidence already identifies unconfirmed installed-mod uninstall (`std::filesystem::remove`) and profile restore-latest overwrite paths; these need confirmation and inert visual routes before release sign-off.
- **Visual completeness P1:** the 165 base fixture routes / 239 capture positions cover primary pages well but omit many meaningful interaction-created surfaces. The expansion inventory includes account setup/recovery, Java install/remove, profile/library/project menus and confirmations, Bedrock destructive dialogs, server file/delete/validation surfaces, mod-manager uninstall/details/dependency surfaces, account/session, Essentials, and state variants. The likely defensible matrix is roughly 225–240 base routes plus scroll/responsive variants, not four screenshots or an unqualified 165-page claim.
- **Visual UX P2 found from fresh evidence:** Quick Search rendered as an oversized empty black panel. The shared search dialog now has a purposeful zero state, no-result guidance, keyboard footer, in-field hint, and fixture-only state reset so a reused process cannot disclose a prior query/history. It needs a fresh capture after rebuild.
- **Supabase security guidance was refreshed against current official documentation:** `SECURITY DEFINER` requires a pinned empty search path plus schema-qualified relations; exposed functions need explicit execution privileges; RLS must accompany exposed tables and ownership predicates must accompany `TO authenticated`. The 2026 changelog contains no directly applicable client SDK breaking change from the inspected index, but it highlights current platform/health advisor changes. Source audit remains required before any migration decision.

## 2026-09-22 audit kickoff

- The repository is on `main` at `767cb0b` and contains a substantial pre-existing uncommitted change set spanning server provisioning, provider integration, Supabase, launcher UI/state, tests, and a new RLS migration.
- The working tree also contains untracked QA helpers, compiled object/executable artifacts, screenshots, logs, and convenience scripts. These are treated as user-owned until their provenance and intended disposition are established.
- The August evidence below predates the September working tree and cannot be used as current build or release proof without fresh verification.
- The current request is architectural in scope. The active work is audit and design only until the user explicitly approves the proposed implementation slice.
- The tracked diff is large (23 files, approximately 1,924 insertions and 579 deletions) and centers on server-provider discovery, runtime provisioning, server UI/state, Supabase verification, and associated tests.
- `git diff --check` found no whitespace errors; Git did report line-ending normalization warnings for the modified files.
- `cpp/build-release` is newer than the August baseline and contains the new `amalgam_server_provision_test`, but its binaries are dated September 16 and therefore still require a fresh rebuild and test run for this audit.
- `cpp/build-release/.amalgam_running` remains from the September 16 QA session. It is runtime state in a build directory, not yet evidence of a product defect; its process/lifecycle context must be checked before any cleanup.
- The production placeholder/TODO scan did not reveal a new obvious unfinished marker in the server-provisioning changes. Matches were primarily legitimate temporary-file handling, tests, comments, or intentional artwork fallbacks.
- The new software catalog and launch-target resolver are a sound shared-foundation direction: UI readiness and start behavior now consult the same on-disk runtime contract, and enum values were appended to preserve serialized compatibility.
- Release-test reliability is improved by undefining `NDEBUG` for test executables; many existing tests use `assert`, so this closes a real false-green risk in Release builds.
- **P1 artifact-integrity gap:** `RuntimeArtifact` records Mojang's SHA-1 and exact size for Vanilla, but the generic `download_runtime` call accepts only SHA-256. The Vanilla runtime path therefore downloads with neither the published SHA-1 nor size verification despite its UI/catalog copy claiming the artifact is verified. The same metadata should flow through one verified download contract and receive a regression test.
- Provider downloads that publish no checksum (for example current Purpur/BungeeCord/Bedrock URLs) remain transport-only trusted. The design should distinguish “provider supplied no digest” from “digest available but discarded,” and surface that honestly in diagnostics.
- The background provisioning state uses atomics plus a mutex and applies server-list mutations on the UI thread, which avoids the most obvious data race. Cancellation is cooperative during network transfer; installer execution itself is not interruptible once launched and should be described that way in UX/tests.
- **BUILD VERIFIED (current September tree):** the configured `cpp/build-release` dependency graph is current; Ninja reported no pending compilation.
- **AUTOMATED TESTED:** all 39 current CTest targets passed in 42.85 seconds, including the new provider/provisioning tests, server transport probe, all Java bridge checks, prerequisite smoke, and client bridge probe.
- Current Supabase guidance confirms the migration's central concern: exposed tables need both explicit grants and row-scoped RLS policies; `TO authenticated USING (true)` is authentication without row-level authorization.
- Current Supabase guidance also raises a second audit requirement: `SECURITY DEFINER` functions should use a pinned/empty `search_path`, should not live in exposed schemas when avoidable, and are executable by `PUBLIC` by default unless privileges are explicitly revoked. The repository's functions and grants need to be checked against that rule, not only its table policies.
- No September 2026 Supabase breaking-change entry appears directly relevant to this RLS migration. The notable current Data API change is the platform-wide move toward opt-in exposure/default grants, reinforcing explicit grants rather than invalidating the migration approach.
- **SOURCE VERIFIED:** `node tools/verify-supabase.mjs` passes across 22 migrations and 24 Edge Functions. It is explicitly a repository-contract check, not proof of the live project state.
- The Supabase CLI is unavailable in the current shell, so database advisors and local DB policy tests cannot yet be run through the CLI.
- **P1 database-hardening debt:** the migration history contains numerous `SECURITY DEFINER` functions in the exposed `public` schema with `set search_path = public` (or `public, auth/extensions`) rather than an empty/pinned safe path. Later migrations revoke broad execution for many, but the final effective privileges and all function bodies need a deterministic audit; the current verifier does not establish that complete property.
- The live verifier is safely locked to the Amalgam project ref and requires a publishable key; it checks auth reachability, expected published tables, and unauthenticated rejection of the protected CurseForge function without printing the key.
- **AUTOMATED TESTED:** the Bedrock add-on validator passes for version 1.0.0, with all seven required script entry points present and the expected behavior/resource UUIDs.
- **P1 product-truth gap:** the checked-in `website/index.html` is a static launch page whose claims have drifted from the current product. It says “8 Server Types,” while the launcher catalog now has 12 offers; advertises “5 AI Models Built-in” and “No cloud API,” while current launcher configuration documents multi-provider cloud failover; and claims a 76 MB installer/11.6 GB local AI stack without current artifact evidence in this audit. Public claims must be generated or release-validated against the actual package/capability manifest.
- README test documentation is stale: it still says all 36 CTest checks must pass, while the current suite contains 39.
- The root contains many ignored QA artifacts plus several untracked root-level helper source/object files. They do not ship because the patterns ignore most compiled/log/image outputs, but the untracked command/source/migration files require explicit classification before release.
- Browser QA reached the live `https://amalgam-mc.com/` site with the title “Amalgam Launcher — Your Minecraft. One Launcher.” The deployed site is not the checked-in static `website/index.html`; it exposes product/network/resources navigation and a different layout/content system.
- **P2 live-site first-impression delay:** two loads reproduced a splash/loading state that hides the entire main page while leaving only header/footer framing visible. The actual hero and content appeared after a six-second wait, with no console exception. This is a deliberate or CSS-driven gate rather than missing content, but it makes a fast static marketing page appear broken for several seconds and should be shortened or removed after measuring real asset readiness.
- After the splash clears, the live home page exposes the expected hero, feature cards, launcher screenshots/tabs, local-server and Essentials sections, FAQ, and full footer navigation. The initial blank-main candidate is therefore resolved into a performance/UX issue rather than a permanent rendering failure.
- The initial axe invocation returned zero passes and logged unrecognized combined tags, so it is not valid accessibility evidence yet; rerun with supported tag syntax/default audit before drawing an accessibility conclusion.
- **P1 live-site layout stability:** the browser performance pass measured CLS 0.40 (one layout shift worth 0.4007), far above the usual “good” threshold of 0.10. FCP was 316 ms, TTFB 117.6 ms, and LCP 1.784 s, so raw delivery is fast; the splash/hero transition is the dominant perceived-quality problem.
- **P2 accessibility:** the valid default axe audit reported three violations: insufficient 2.79:1 contrast on Privacy Policy and Terms links (serious), heading-order jumps in server/Essentials feature blocks, and a viewport meta tag that disables mobile zoom (`maximum-scale=1`). It also left six contrast cases incomplete because layered backgrounds prevented deterministic calculation.
- The live home page adapts its accessibility tree to a 390x844 viewport without losing the main navigation trigger, hero, feature controls, FAQ controls, or footer links. Visual mobile inspection is still required for clipping/overflow.
- **VISUAL QA:** the 390x844 hero is legible and does not horizontally clip, but the primary “Explore features” action is pushed almost to the bottom of the first viewport by a large empty gap. This weakens the mobile first screen after an already long splash and belongs in the same hero-density refinement.
- The live `/download` page is honest rather than fake: it clearly reports “NOT CONFIGURED / WAITING FOR BUILD” and offers Preview Lab instead of presenting an unverified installer. This correctly gates public distribution.
- **VISUAL QA (current launcher binary):** Servers list, server detail, minimum-size server detail, and minimum-size profile Content routes all rendered successfully from deterministic fixtures. The server surfaces are dense, aligned, and keep their primary actions/metrics readable at the 960x600 minimum.
- **P2 launcher empty-state polish:** the profile Content route at 960x600 shows a large empty panel with only “No content matches your filters.” when the profile has zero installed content. The blank search control plus a label rendered to its right is also less clear than an in-field prompt. This should become a purposeful “No content installed” state with a concise explanation and Add/Upload actions; the filtered-empty copy should be reserved for an active query/filter.
- The 1600x900 snapshot request was capped by the available desktop to roughly 1242x610, so this pass provides minimum/medium visual evidence, not true 1600x900 or 1920x1080 coverage.
- **P1 release-gate false assurance:** `tools/secret-scan.mjs` consumes only `process.argv[2]`, but `release-gate.ps1` invokes it with `tools installer cpp/launcher`. Node therefore scans only `tools`; the gate can print “secret scan passed” without examining the installer or launcher sources it names. The scanner should accept all path arguments or the gate should invoke it once per root, with regression coverage.
- **AUTOMATED TESTED:** the runtime-agent suite passed 24/24 tests, and the remaining Node tooling suite passed 39/39 tests.
- **SECURITY SCANNED:** explicit scans of `tools` (63 text files), `installer` (6), and `cpp/launcher` (121) found no secret-category matches. This establishes the real result even though the combined release-gate invocation is currently wrong.
- The active `cpp/build-release/launcher.json` contains a Microsoft client ID but no complete Supabase URL/publishable-key pair and no CurseForge key. Consequently, live launcher-to-Supabase persistence and CurseForge runtime checks remain externally/configuration blocked in this build, while Microsoft publisher/account approval still determines whether end-to-end sign-in can complete.
- Direct PowerShell invocations of the GUI-subsystem launcher did not expose `$LASTEXITCODE` or console output for `--doctor`/`--check-prereqs`; those checks are already covered by passing CTest smoke targets, but a separate waited-process probe is needed for fresh CLI exit evidence.
- **LIVE CLI VERIFIED:** waited hidden-process probes returned exit 0 for both `--doctor` and `--check-prereqs` against the current Release launcher.
- A September 16 disposable live-provision log shows successful real provider installs for Paper 1.20.1, Fabric 1.20.1, Bedrock Dedicated Server, and Forge 1.20.1, with config files and launch targets verified. This is valuable historical live evidence, but it predates this audit and does not close the newly identified digest-verification gap.
- The working tree still carries throwaway probe sources, compiled objects/executables, a PID file, backup JSON, screenshots, and root convenience scripts. Most binaries/logs are ignored, but the untracked source/command files and the security migration would be easy to omit from a commit. Release refinement should classify them into: product/test assets to promote under `tools`/`cpp/tests`, documentation evidence under `artifacts`, and disposable debris to remove recoverably.
- **P1 Forge/NeoForge launch defect:** the start command concatenates `@<server-dir>\user_jvm_args.txt` and `@<server-dir>\...\win_args.txt` without quoting. The UI allows spaces in server names (for example the fixture “Forsaken World SMP”), so normal `%LOCALAPPDATA%\amalgam\servers\<name>` paths can split into multiple JVM arguments and prevent loader servers from starting. Existing tests create hyphenated temporary paths and do not cover this case.
- **P1 create-flow crash/input defect:** server-name validation rejects path punctuation but not Windows reserved device names (`CON`, `NUL`, `COM1`, etc.), trailing spaces/dots, or path aliases. The UI then calls the throwing `std::filesystem::create_directories(dir)` overload on the render thread with no exception boundary. A valid-looking reserved name can therefore terminate the launcher instead of producing a validation error.
- **P1 provisioning exception defect:** `provision_runtime` uses throwing directory creation for both the server and scratch directories despite its API contract returning `false` plus an error. Disk/permission/path failures can escape the worker thread and terminate the process. `write_server_config_files` already demonstrates the correct non-throwing error-code pattern.
- The current provisioning tests cover manual-runtime messaging, config-file creation, and an unusable config directory, but they do not cover verified-download metadata propagation, spaces in loader argument-file paths, Windows reserved names, or provisioning-directory failures.
- **P2 cancellation semantics:** the UI continues to present “Cancel download” while a loader installer is executing, but cancellation is only observed by network progress callbacks. The installer process is not interrupted; if it succeeds, the job is reported successful even after Cancel was pressed. The UI should either support process cancellation or switch to a truthful non-cancellable “Installing” phase.
- The repository tracks only `website/index.html`; it does not contain the source that produced the current live React-style site. Live-site fixes can be specified and verified here, but implementation requires the actual website project/deployment source to be placed in scope.
- `supabase/tests` is absent. The source verifier is useful, but current Supabase guidance calls for allow/deny policy tests; effective RLS/function security is not release-certified without a local or linked-project database test path.
- Final `git diff --check` remains clean aside from line-ending normalization warnings; no whitespace defect was introduced by the audit documentation.

## Verified passes

- Fresh MSVC Release build completed successfully in `cpp/build-vs`.
- CTest completed with 36/36 tests passing.
- `bedrock/AmalgamBedrockClient/tools/validate.js` completed successfully.
- Live navigation and rendering were checked for Home, Discover, Modrinth project details, Library, Downloads, Essentials, Servers, Settings, and Bedrock.
- Modrinth discovery returned real featured projects, mods, detail content, changelogs, and version releases.
- A real Modrinth mod install completed into a disposable profile with a verified jar.
- A real Modrinth modpack install completed into a disposable isolated profile with 48 mods and 3 resource packs; the profile metadata, loader, and selected Minecraft version were verified.
- A bounded Java Forge launch dry-run resolved the loader, Java 17, client, natives, assets, native DLL injection, and bridge mod without spawning gameplay.
- The project server transport fixture passed against the fresh launcher: file operations, path traversal rejection, console stdin/stdout, and graceful stop.
- Live Supabase health passed with the supplied publishable key: Auth endpoint and protected CurseForge function behavior were correct.
- Bedrock Overview, Profiles, Worlds, Backups, and Add-ons tabs rendered with their empty states and controls.
- Login and registration surfaces opened and closed through their own controls without requiring a launcher restart.
- Profile creation, server creation, and Bedrock profile wizards opened, rendered, and cancelled correctly.
- The recovery dialog close-state defect was fixed in `cpp/launcher/src/ui.cpp`, rebuilt, and retested.
- A clean Alt+F4 shutdown removed `.amalgam_running`; a subsequent launch did not show a false recovery dialog.
- Bedrock package detection was narrowed to the Bedrock UWP package and launch activation was corrected to the current Windows AUMID (`!Game`); `--bedrock-launch` returned success and the test process exited cleanly.
- A strict fresh Release build completed in `cpp/build-release`; the archive, extracted runtime, Inno installer, and installed-package prerequisite/Home smoke checks passed.
- The minimum-size visual route matrix completed at 960x600 for 22 launcher routes; every route rendered and exited successfully, and the review found only legitimate long-content scrolling.
- Cached launch validation completed successfully for Fabric, Quilt, NeoForge, and Forge across the supported 1.12.2, 1.18.2, 1.19.2, 1.20.1, and 1.21.8 cases.
- A clean native real-user QA session completed live navigation through Home, Discover, Library, Downloads, Essentials, Servers, and Settings, including global search and wheel checks; the session exited cleanly with no reproducible clipping or crash.
- The compact-window focused check reproduced and fixed a shared shell defect where the sidebar accepted page-wheel input; the rebuilt executable keeps the logo/navigation fixed, while a fitting Settings section does not move unnecessarily.
- The updated Inno installer was tested from a fresh disposable directory: silent install, packaged prerequisite check, installed Home snapshot, and silent uninstall all passed; user-created config/log/snapshot files were intentionally preserved.
- Release smoke tooling was corrected to accept an explicit package directory; the current fixed-sidebar package passed both prerequisite and Java smoke checks through that path.
- A second disposable installer run covered same-version repair/upgrade: the first-launch config hash remained unchanged, post-upgrade prerequisites passed, and silent uninstall removed binaries while preserving the user-created config.
- The desktop render loop was frame-paced at approximately 60 FPS; the previous one-millisecond delay left rendering effectively uncapped and allowed the shell to compete with Minecraft for CPU/GPU time.
- Recurring profile content, world-count, screenshot-count, Library-world, and Screenshots-gallery directory scans were moved to background workers with generation checks so stale results cannot overwrite a newly selected profile.
- The updated Visual Studio Release build passed all 36 CTest cases, clean extracted-package runtime validation, and packaged launcher prerequisite/Java smoke checks without opening the launcher window.

## Findings still open or unverified

1. The current `cpp/build-vs/launcher.json` has no active CurseForge runtime credential or proxy configuration, so CurseForge discovery could not be verified in this build.
2. The current runtime has no Microsoft client ID, so Microsoft/Xbox authentication could not be exercised end to end.
3. The current runtime has no active Supabase configuration, so launcher-to-website account persistence could not be exercised end to end.
4. CurseForge discovery and installation remain unverified because the active runtime has no CurseForge key or approved proxy configuration.
5. A real Modrinth modpack was imported and validated, but a playable authenticated Minecraft launch was not completed in this pass.
6. The local server transport journey passed, but a full Minecraft server create/download/start/restart journey and long-running server health check remain unverified.
7. Bedrock detection and launch now pass, but companion-client behavior and online/offline gameplay remain unverified; Microsoft account approval is still external.
8. Supabase live endpoints pass, but the installed launcher template is intentionally unconfigured, so launcher↔website account persistence remains unverified in this runtime.
9. Disposable install/launch/repair-upgrade/uninstall passed after the silent-uninstall fix, but a separate Windows test-account rollback run and public signing certificate validation remain unverified.
10. The demonstrated compact scroll-ownership defect is fixed: the sidebar no longer captures page-wheel input. The Settings section remains stationary when its content fits; further minimum/maximum DPI coverage is optional release hardening, not a known defect.
11. One desktop focus-helper timeout occurred during probing; repeated process checks and runtime logs did not show a reproducible product crash.
12. This performance pass was validated headlessly because the user was actively playing a game; no new foreground visual session was run. A follow-up idle CPU comparison and screenshot traversal should be run when the user is free to test the installed build.

## Constraints

- Provider credentials and service keys must stay outside source control and outside generated code.
- The release must preserve the existing 19 Java bridge artifacts and Bedrock package validation rules.
- No custom Minecraft packets may be introduced.
- A feature is not considered complete until its runtime path, error path, and clean shutdown path are tested.

## 2026-09-23 — Risk-first stabilization facts

- **FIXED / TESTED:** `amalgam_auth_test` now redirects only its own process to a unique temporary local-app-data root before exercising save/load/logout. The focused test passed, and a metadata-only check confirmed it did not create a normal local account file. The earlier unsafe test design must not be used as release evidence.
- **FIXED / TESTED:** runtime telemetry now reports unavailable dynamic values as unknown/null rather than fabricated zeroes. `npm test --prefix tools/runtime-agent` passed 38/38; `node tools/verify-supabase.mjs` passed its local source verification for 25 migrations and 24 Edge Functions. Neither check validates the live Supabase project.
- **FIXED / TESTED:** `tools/update-manifest.mjs` no longer generates an implicitly unsigned/size-less production manifest, and its CLI verifier binds manifest signature plus optional payload size, SHA-256, and payload signature. `node --test tools/update-manifest.test.mjs` passed 12/12.
- **P0/P1 IN PROGRESS:** local Amalgam session persistence is being made transactional; ordinary launcher sign-out is being changed to explicit Supabase local scope; account UI flows are being consolidated behind revalidated confirmation actions; and Amalgam sign-out no longer conflates or clears the separate Minecraft identity.
- **VISUAL-EVIDENCE FACT:** the newest broad launcher folder contains 53 PNGs, not four, but only 53/239 planned entries, all top checkpoints. Images are physically 1242x610 while filenames describe 1256x624; there is no complete capture ledger. The run predates relevant UI changes and cannot support a final UI-quality claim.
- **EXTERNAL RELEASE BLOCKER:** the checked-in local update feed does not bind the current ZIP; final signing, a publisher-confirmed payload host, and post-upload endpoint verification require release authority outside this repository pass.

## 2026-09-23 — Stabilization verification update

- **FIXED / TESTED:** The account-action coordinator now keeps Amalgam local sign-out, remembered-local-sign-in removal, all-local-removal, and Minecraft disconnect as separate, confirmed actions. It uses explicit Supabase local sign-out scope, snapshot/revalidation before execution, protected local-session persistence before remote invalidation, and no longer treats a local remembered sign-in as a remote "device session." Seven deterministic fixture routes cover its confirmation, ready, stale, and local-error states.
- **FIXED / TESTED:** Java-profile resolution now validates an explicitly configured Java home/bin/executable against the required major version before launch. Native extraction uses a content-fingerprinted completed layout rather than mutating/reusing a shared native directory; archive exclusions and traversal-shaped exclusions are covered by tests.
- **NATIVE REGRESSION BASELINE:** CTest is green for all 42 configured checks, verified in two bounded runs: 18/18 and 24/24. This includes the isolated auth test, new Java/native-layout tests, launcher prerequisites, all supported Java bridge contract checks, server transport, and client bridge probe.
- **RELEASE TOOLING FIXED / TESTED:** packaging/signing tooling now uses a byte-safe staged sequence: sign staged launcher/DLL, regenerate component metadata and final ZIP from those exact bytes, build/sign the installer, then generate/sign/verify the final update manifest. Tooling fails closed for zero discovered tests, unclassified inert/production release runs, missing inputs, and component hash/signer mismatches. No real signing, private-key use, endpoint change, upload, deployment, or official-site modification occurred.
- **EXTERNAL RELEASE GATES:** Publisher signing identity/timestamping, matching updater key authority, a publisher-approved final-ZIP HTTPS endpoint, post-upload byte verification, clean-machine validation, real entitled Minecraft/Bedrock journeys, and CurseForge production credentials remain outside the repository-only pass.
- **VISUAL MATRIX VERIFIED:** the current source registry has 172 deterministic fixture tokens and the manifest has exactly 246 primary planned routes (172 top plus 74 scroll checkpoints), with 166 compact and 190 small responsive high-risk checkpoints. The matrix is structurally synchronized but remains uncaptured until the hardened capture ledger and an honest non-clamped viewport are available.

## 2026-09-23 — Current source stabilization / evidence status (build pending)

- **FIXED IN SOURCE / AWAITING BATCH BUILD:** the Local Servers detail page could set a remove action and return before the global confirmation was drawn. The confirmation is now shared by list/detail composition, revisions are persisted before in-memory removal, and a write failure keeps the entry visible with an inline retryable error. Copy now accurately distinguishes removing an Amalgam entry from deleting server files.
- **FIXED IN SOURCE / AWAITING BATCH BUILD:** Local Servers file deletion no longer closes its confirmation after a filesystem error or a missing target. Partial directory deletion is explicitly disclosed; errors preserve the dialog and require the user to refresh before retrying.
- **FIXTURE EVIDENCE EXPANDED / AWAITING FRESH EXECUTABLE:** account creation, verification, recovery, sign-in error/loading, and Microsoft device-code routes now have synthetic, non-network composition; server file-delete, restore-backup, and remove-entry confirmation states do too. The source manifest currently has 203 accepted route tokens, 277 canonical captures, and 695 viewport/scroll captures. The currently built executable predates those additions, so the hardened runner correctly refuses to capture it.
- **RENDER-PATH P1s IN IMPLEMENTATION:** authentication flows and Essentials/social request paths are being moved to generation-guarded joined workers so a slow provider or relay cannot freeze the launcher or update a closed/replaced dialog. They are not marked tested until the shared rebuild and regression pass complete.
- **FOLLOW-UP P1 IDENTIFIED:** Account Security reads remote security settings synchronously during page rendering, while Account Activity can query remote activity and Account Statistics scans instances per frame. These passive account surfaces need a cached, generation-guarded refresh lane after the current explicit auth-flow pass; they are not solved merely by making submit actions asynchronous.
- **VISUAL COVERAGE GAP REMAINS EXPLICIT:** existing parent-tab fixtures do not yet constitute proof for Bedrock world/backup/add-on destructive dialogs, mod uninstall/details/dependency graph, cache cleanup, profile recovery, managed-Java changes, or admin confirmations. Those are the next priority evidence set rather than unreviewed hidden work.

## 2026-09-23 — Exhaustive interaction-fixture audit

- **CURRENT REGISTRY PARITY, NOT COMPLETENESS:** `ui.cpp` registers 221 deterministic fixture tokens and `tools/launcher-visual-qa-cases.full.json` contains 296 case records with 27 middle and 48 bottom checkpoints. The registry and manifest have zero missing/unknown tokens. This proves the existing fixture inventory is synchronized, not that every live interaction is represented.
- **ACTIVE-SURFACE EVIDENCE GAPS:** the current matrix lacks deterministic routes for profile restore-latest/delete-restore-point/restore-game-options confirmations and their retry/error views; Mod Manager recovery move, detail and dependency dialogs; Server backup-restore/remove failure states and file preview/context; typed/reasoned Admin confirmation; Essentials friend-profile/context; cache cleanup; content-install/version/creator-update/library-group/managed-Java/admin-password dialogs; and remaining reachable overflow/context/combo menus. The compiled `Switch Account` popup is not counted as a gap because no production source path opens it.
- **FIXTURE-SAFETY P1:** fixture mode is not yet a sufficiently strong action boundary for all existing Server overlays. `draw_remove_server_dialog` can write the local server list and `draw_server_files_tab` can remove a path after a fixture-rendered confirmation if clicked; backup restore instead writes a fixture error but leaves its action enabled. The next implementation must disable all fixture-mode action controls and guard the mutation entry points themselves, then apply the same fail-closed pattern to every new fixture route.
- **CAPTURE-RUNNER HARDENING VERIFIED:** the visual runner now records bounded fresh isolated retries only for an exit-0 exact-1x1 artifact or a material clamp, retaining original/retry images, logs, IHDR, hashes, and child isolation in JSON and Markdown. A normal four-capture proof passed; a deliberate oversize proof kept both 1242x610 attempts and failed rather than being relabeled 1600x900. The interrupted 91-artifact broad run is preserved as diagnostic evidence only and is not a pass.

## 2026-09-23 — Fixture expansion integration checkpoint

- **FIXED IN SOURCE / AWAITING BUILD:** fixture-mode Server remove-entry, file-delete, and backup-restore presentation is now fail-closed at the action boundary as well as visually disabled. The new static `server-file-preview` never reads a server file or invokes Explorer; its `@bottom` route has a component-owned scroll body.
- **MATRIX EXPANDED:** the source registry and JSON manifest now agree on 261 tokens and 337 case records, up from 221/296. The 40 newly represented interaction surfaces are all local/inert and all new top routes are in both 960x600 and 1256x624 high-risk selections; the additional server-preview bottom route is included too.
- **EVIDENCE NOT YET CLAIMED:** these integrated source changes have not yet been compiled, regression-tested, or visually captured. The prior 91-image run remains diagnostic and cannot certify the expanded matrix.

## 2026-09-23 — Final local UI evidence and remaining certification scope

- **BUILD VERIFIED:** the final Release launcher build completed successfully after the fixture/modal fixes.
- **AUTOMATED TESTED:** CTest passed in two current bounded batches, 22/22 and 22/22 (44/44 total). The update-manifest Node suite passed 12/12.
- **VISUALLY TESTED:** the final immutable launcher capture run recorded 792 passed, zero failed, and zero blocked at requested 1256x624 and 960x600 host-valid tiers (actual client areas 1242x610 and 946x586). No transient retry/recovery was needed.
- **PROVENANCE VERIFIED:** final capture and review ledgers confirm launcher-binary, manifest-copy, fixture-inventory, and case-selection hashes match; configuration isolation passed and launcher.json was unchanged.
- **FULL-PAGE/STATE EVIDENCE:** the review generated 257 contact sheets. Its ledger reports 792 valid images with zero missing images, invalid images, hash mismatches, dimension mismatches, or provenance issues.
- **CURRENT LOCAL CONCLUSION:** no repository-resolvable visual blocker is known in the deterministic fixture matrix. This is not a claim that production sign-in, real Minecraft launch, Bedrock import, live Supabase/Essentials, signing, hosted updater, or clean-machine distribution has been certified.
- **NEXT SCOPE:** package/provenance, disposable installed-copy, and local operational-readiness certification. The official Amalgam website and live Minecraft network must not be accessed or changed in this pass.

## 2026-09-23 — Release-completion audit findings

- **CURRENT PROVENANCE VERIFIED:** the final ledger's launcher SHA-256, 01e31dca30def65606e317cc5199cf56b5ed8cac471b888eca45ce14933956a0, matches the current cpp/build-release/amalgam_launcher.exe. The captured and current visual manifests share SHA-256 efc2490d78200c3d2140e3b4003c6d256d484e48e2c275c22370749100f323c1.
- **STALE DISTRIBUTABLES:** dist/amalgam-1.0.0, dist/amalgam-1.0.0.zip, and dist/installer/AmalgamLauncher-1.0.0-Setup.exe are dated 2026-09-15. The staged launcher hash is 077a7bbf3889b2a41109ef5a770dc6676d4054466f6dbe5abbb0ea871b3194dd, so none may be called a candidate for the final visual-certified source.
- **FRESH LOCAL GATES:** current CTest passed 44/44 in two bounded batches. The current update-manifest test passed 12/12; runtime-agent passed 38/38; Supabase source verification passed for 25 migrations and 24 Edge Functions; Bedrock truth-correctness passed 4/4 and its validator passed.
- **P1 UPDATER TRANSACTION DEFECT:** updater prepare_apply extracts the full ZIP but only requires the root launcher EXE, then generated helper lines back up/copy only that EXE. rollback_update also restores only the EXE. A signed package includes the DLL, bridges, Bedrock package, branding, runtime assets, and manifests, so this can leave a mixed-version installation. The update feed must remain unavailable until application and rollback are component-aware, transactional, path-safe, and test-covered.
- **P1 SERVER READY TRUTH DEFECT:** an installer process exiting zero does not by itself establish that a local server has a generated runnable target. Installer provisioning must validate its resolved launch target/runtime before UI reports Ready.
- **P2 DIAGNOSTICS TRUTH DEFECT:** diagnostics checks for a nonexistent sibling updater.exe even though updating is in-process. Its Essentials/Supabase status currently uses a Modrinth request, which is neither an Essentials nor a backend health check. The normal UI invokes diagnostics synchronously, so direct network checks can also freeze the render thread.

## 2026-09-23 — Active release-blocker repairs

- **UPDATER P1 — FIX IN PROGRESS:** Read-only repro analysis confirms that a signed full release archive is extracted but the helper only backs up and swaps `amalgam_launcher.exe`; the active install can therefore become a hybrid of new executable plus old DLL, bridges, Bedrock payload, and managed assets. The repair scope is a manifest-controlled, path-safe complete-component transaction with whole-tree rollback coverage in isolated tests. No real installation or update feed has been used.
- **SERVER READY P1 — PATCH READY / REVIEW PENDING:** The scoped server repair adds explicit EULA acceptance and runnable-runtime verification before config writes or a Ready state. Installer-style provisioning with exit code zero but no valid launch target should now produce an actionable Error state. Source review, native build, and targeted regression verification are pending.
- **SERVER READY FOLLOW-UP — FIX IN PROGRESS:** A preexisting readiness helper treated an existing directory named `server.jar`, `win_args.txt`, or `bedrock_server.exe` as a present runtime. The resolver now requires a regular file for every launch-target form, with focused state tests for ordinary JAR, Forge argument-file, and Bedrock executable directory impostors. Build/test verification remains pending with the broader server batch.
- **SERVER CONFIG DATA-LOSS FOLLOW-UP — FIX IN PROGRESS:** A successful repair/Prepare flow re-invoked the default config writer and could overwrite an imported server's existing `server.properties`. The writer now creates defaults only when that file is absent and preserves an already accepted EULA file; an isolated regression asserts exact preservation of owner-managed properties/EULA text. Build/test verification remains pending.
- **CERTIFICATION BOUNDARY:** Existing ZIP/installer artifacts remain stale and must not be reused. A fresh candidate will be created only under a unique disposable artifact root after the current source batch is reviewed, built, and tested.
- **SOURCE/PACKAGE-INPUT SECRET SCAN — PASS:** The repository scanner's own tests passed 2/2. It scanned 522 text files under the launcher, Bedrock client, Java bridge trees, Supabase source, tools, and installer input without printing matched values; result: 0 secret-category matches, including 0 unexpected HIGH findings. This does not replace the required scan of the future final candidate package.

## 2026-09-23 — Current controlled-candidate blockers and repair ledger

- **P2 DIAGNOSTICS REPAIR — REVIEWED / AWAITING BUILD:** diagnostics is now explicitly local-state-only. It no longer probes Modrinth, a backend, providers, or a fictitious `updater.exe` during UI rendering; instead it reports the in-process updater policy, local configuration validity, and session prerequisites. The added deterministic coverage uses in-memory configuration only. A narrow UI follow-up will ensure an invalid non-HTTPS endpoint also avoids reading the backend singleton.
- **P1 DESKTOP BACKEND-KEY MODEL — REPAIR IN PROGRESS:** the desktop configuration and manager still accept a legacy `supabase_service_key` field even though no client path needs it. The active repair removes privileged-key flow from the desktop client, fails safely for an old configuration, and adds inert regression coverage without opening any user configuration or backend connection.
- **PACKAGE-INTEGRITY REPAIR — IN PROGRESS:** the release packager validates the Bedrock archive structure/version but did not independently compare the archive digest with both supplied metadata records. A scoped local-tool repair is adding that fail-closed comparison; no package has been generated or overwritten.
- **FRESH-INPUT NO-GO:** `cpp/build-release/bridges` has all 19 expected bridge names, but four Forge copies are older than their local build outputs and will fail the packager's freshness rule. The existing Bedrock add-on metadata/hash trio is internally consistent but predates current Bedrock source changes. A candidate must rebuild/re-stage inputs and validate the exact resulting component hashes before packaging.
- **CANDIDATE-IDENTITY BOUNDARY:** no release version or public identity has been changed. If the current `1.0.0` identity is retained for local certification, it will be staged only in a new uniquely named artifact root; changing a published semantic version requires an intentional release decision rather than an audit-side mutation.

## 2026-09-23 — Java test authorization / Bedrock exclusion

- **USER-AUTHORIZED JAVA SCOPE:** a Java Edition profile handoff/launch verification may use the existing signed-in official Minecraft Launcher state after the local candidate passes build and safety gates. The current Java Minecraft session is active, so it must not be focused, altered, closed, or replaced during preflight.
- **BEDROCK RUNTIME EXCLUDED:** the user reports Minecraft for Windows can crash this PC. No Bedrock detection, launch, add-on import, world access, or other runtime action is authorized. Existing static Bedrock archive/version/hash evidence remains package-only and must not be presented as a runtime certification.

## 2026-09-23 — Integrated source-repair verification

- **NATIVE BUILD / REGRESSION PASS:** the integrated `cpp/build-release` CMake build completed after the updater, server-provisioning, diagnostics, and desktop-key changes. The prescribed split CTest runs passed 22/22 and 22/22, for 44/44 total. This includes `amalgam_updater_test`, `amalgam_config_test`, `amalgam_diagnostics_outbox_test`, `amalgam_server_run_state_test`, and `amalgam_server_provision_test`.
- **LOCAL TOOL / CONTRACT PASS:** update-manifest passed 12/12; runtime-agent passed 38/38; the local Supabase verifier passed for 25 migrations and 24 Edge Functions; source-secret scanner tests passed 2/2 and a 522-file scoped scan produced 0 secret-category matches; the package scripts parsed successfully.
- **UPDATER REPAIR — FIXED / TESTED (LOCAL):** a staged signed archive is now checked as a full allowlisted component set before a helper is generated. The helper and C++ rollback plan cover product-managed EXE/DLL/bridges/Bedrock/assets/metadata and preserve live configuration/profile/instance paths. The test proves the plan/rollback contract in disposable fixtures; it does not substitute for later installed-copy helper execution.
- **SERVER / DIAGNOSTICS / DESKTOP-KEY REPAIRS — FIXED / TESTED (LOCAL):** regular-file runtime truth, explicit EULA/config preservation, local-only diagnostics, HTTPS-gated diagnostics singleton access, no desktop service-role key flow, and legacy-field safe inertness all passed their compiled regressions. UI-facing server/diagnostics changes still require fresh focused fixture capture before visual evidence is renewed.
- **SOURCE-KEY CONTRACT:** the remaining `supabase_service_key` textual references are intentional legacy-field detection, compile-time rejection guards, inert test fixtures, a package rejection rule, and security documentation. No runtime desktop key field or manager argument remains.

## 2026-09-23 — Official launch gate findings

- **LAUNCH DECISION POLICY:** repository-resolvable P0/P1 defects must be closed and retested before a GO. A complete local candidate can still be reported as `TECHNICALLY READY — EXTERNAL OWNER GATE` when Microsoft production approval, trusted code signing, publisher-controlled update hosting, or live production credentials are unavailable.
- **JAVA TEST BOUNDARY:** Java-only official-launcher preflight/profile handoff is authorized after the candidate is ready. An active Java Minecraft process must not be focused, closed, replaced, or modified. The current preflight must be non-disruptive.
- **BEDROCK TEST BOUNDARY:** no Bedrock detection, launch, import, world/add-on access, or runtime certification is permitted. Static archive/package/hash/inventory validation may remain part of package integrity evidence only.
- **VISUAL MATRIX CLOSURE:** source/manifest parity is currently 335/335 fixture tokens and 436/436 case IDs. Java-only selection is 392 cases at the primary host-valid tier plus 341 compact/small high-risk cases, for 733 planned captures across the two available viewport tiers. Remaining evidence gaps are tooltip, open-combo, toast, alternate-theme, and explicit DPI states.
- **CURRENT SOURCE POLISH:** Configuration now uses the entitlement-gated five-theme selector, production Configuration's advanced card is page-scroll-owned, Diagnostics uses a single page scroll path, and Server fixture notices wrap at compact widths. These changes invalidate older visual evidence and require a fresh build/capture.
- **CANDIDATE RULE:** existing release ZIPs/installers are stale after the current UI changes. No old artifact may be called final; the next candidate must use a unique artifact root and be validated, scanned, hashed, and immutable before certification.
- **LAUNCH-READINESS EVIDENCE (2026-09-23):** native C++ build is current and CTest is 45/45 green; runtime-agent is 38/38; update-manifest is 12/12; Supabase source verifier reports 25 migrations and 24 Edge Functions; scoped secret scan has no unexpected HIGH findings; Bedrock truth test is 4/4 and static validator passes.
- **FRESH VISUAL EVIDENCE:** 733/733 Java-only captures passed across 1256x624 and 960x600. Four partition ledgers sum to 733 captures (184 + 186 + 181 + 182), and the four reviewed galleries report 733/733 valid images with zero integrity attention. Bedrock was excluded from runtime capture by explicit user instruction.
- **CANDIDATE CERTIFICATION:** candidate `rc-20260923T150234478Z` passed ZIP/package validation, clean-extracted runtime validation, installer-input QA, candidate secret scan, installer compilation, CLI `--check-official-launcher`, `--check-java`, `--check-prereqs`, isolated official handoff probe, and disposable install/check/uninstall. Candidate launcher/DLL hashes match the current build outputs.
- **SIGNING / EXTERNAL GATES:** component-manifest binding is clean, but the launcher, DLL, and installer are all `NotSigned`; a trusted production certificate/timestamp is still required. Microsoft approval, live deployment/update publication, and real authenticated Java handoff remain external gates; no public promotion was performed.

## 2026-09-23 — Post-polish candidate and evidence closure

- **FULL NATIVE REGRESSION PASS:** after the final fixture/theme/transient-state changes, the documented Release build remained current and the complete CTest registry passed 45/45.
- **FRESH JAVA-ONLY VISUAL PROOF:** the refreshed matrix at 1256x624 and 960x600 produced 751/751 passed captures across four isolated ledgers. All four visual-review galleries report zero missing, invalid, dimension-mismatched, hash-mismatched, or provenance-invalid images. Bedrock-labelled runtime capture was intentionally excluded.
- **TRANSIENT UI PROOF:** the five-theme selector now has a deterministic open-picker capture showing the two free themes and three entitlement-gated themes with clear locked-state copy. Light, midnight, solarized, Dracula, success/error/stacked toast, and tooltip states were also captured; the dedicated picker review reports 2/2 valid images with zero integrity attention.
- **CURRENT CANDIDATE:** `artifacts/release-candidate-2026-09-23/rc-20260923T231319050Z` is the first candidate containing the latest polish source. Its launcher/DLL bytes exactly match `cpp/build-release`; package validation, clean-extracted runtime checks, installer-input QA, Inno Setup compilation, component-manifest binding, CLI smoke checks, and isolated Java official-handoff probe passed.
- **CURRENT HASHES:** launcher `E454EF0A87EFD9C180D40DF81A8E429C176404CDF8AFC277C236F06E8B957DAE`; DLL `AB7A209992904B13659CAC0502598D52E4705855F1C2BB26735C434C9DEAE5EE`; ZIP `341647DDE7A5C330030814C20D57F68BD0952A92167F8074BC3F247D40AD1A0F`; installer `7ACC6B019B0264F06DDEBAC89D8147ACCCB396C842E487C9779DB0596A063E95`.
- **CANDIDATE SECRET SCAN:** the staged package scan examined 29 text files and found zero secret-category matches. The broader source scan completed with no unexpected HIGH finding; its only outputs were the documented test fixture/API-assignment medium match and expected vendor private-key fixture.
- **INSTALLER SAFETY BOUNDARY:** the existing machine still contains an Amalgam QA installation with two uninstall registrations. I did not replace or silently remove it just to repeat a destructive installer simulation. Earlier disposable install/check/uninstall evidence remains recorded for the previous candidate; a current-candidate install simulation requires explicit clearance or authorization to replace that QA install.
- **HANDOFF PATH NOTE:** the candidate's long workspace path exceeded the Windows path limit for the isolated official-handoff probe. The same probe passed with a short temporary root, returned `READY`, and preserved the active Java session. This is a tooling/path-length constraint, not a product handoff failure.
- **FINAL LOCAL CONCLUSION:** no repository-resolvable UI, packaging, native-regression, or Java-only preflight blocker is known. Public distribution is still not a GO until trusted signing/timestamping, Microsoft production approval, publisher-controlled update hosting/signature, live Supabase/provider deployment, and real authenticated Java handoff are supplied. Bedrock runtime remains excluded.
